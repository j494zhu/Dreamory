# ── Web ───────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# ── Config ────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ── DB: Postgres + pgvector ───────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
greenlet==3.1.1

# ── IDs ───────────────────────────────────────────────
uuid7==0.1.0

# ── LLM (DeepSeek is OpenAI-compatible) ───────────────
openai==1.59.5

# ── Embedding: bge-m3 (optional, heavy). ──────────────
# Install separately if you have a GPU / want the real model:
#   pip install FlagEmbedding torch
# Without it the system falls back to a deterministic hash embedder
# (set EMBEDDING_BACKEND=fallback). See app/llm/embeddings.py.
# FlagEmbedding==1.3.4

# ── Caching (W-TinyLFU / LRU-K family) ────────────────
cachetools==5.5.0

# ── Clustering for Dream (offline) ────────────────────
scikit-learn==1.6.1
numpy==2.2.1

# ── Tests ─────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.2
